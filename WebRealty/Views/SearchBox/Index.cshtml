@model IEnumerable<RealtyDomainObjects.PropertyType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PropertyTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PropertyTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}
</table>

@Html.Partial("_SearchBox", Model)
@*
<div class="searchBox">
    <div class="searchBoxInner">
    @Html.DropDownList("PropertyType", new SelectList(Model, "Id", "PropertyTypeName"), new { data_propertyAction = "searchbox/PropertyAction", @class="searchBoxDDL" })
    </div>
    <div id="PropertyTypeActionDiv" class="searchBoxInner">
    @Html.DropDownList("PropertyTypeAction", new SelectList(string.Empty, string.Empty), new { data_propertyAction = "searchbox/PropertyCities", @class="searchBoxDDL" })
    </div>
    <div id="PropertyTypeCitiesDiv" class="searchBoxInner">
    @Html.DropDownList("PropertyTypeCities", new SelectList(string.Empty, string.Empty), new { data_propertyAction = "searchbox/PropertyCityDistricts", @class = "searchBoxDDL" })
    </div>
    <div id="PropertyTypeCityDistrictDiv" class="searchBoxInner">
    @Html.DropDownList("PropertyTypeCityDistrict", new SelectList(string.Empty, string.Empty), new { @class = "searchBoxDDL" })
    </div>
</div>
*@

@section scripts{
@Content.Script("cascadeSB_DDLs.js", Url)
}

